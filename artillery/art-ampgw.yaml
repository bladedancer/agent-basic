
config:
  target: "https://nginx.gmdemo.sandbox.ampc.axwaytest.net"
  tls:
    rejectUnauthorized: false
  #http:
  #  pool: 10
  http:
    timeout: 30
  phases:
    - duration: 60
      arrivalRate: 600
      name: Sustained load

scenarios:
  - name: "Get hello."
    flow:
      - get:
          url: "/demo/hello"
