
config:
  target: "https://nginx.ampgw.com:8443"
  tls:
    rejectUnauthorized: false
  http:
    pool: 10
    timeout: 30
  phases:
    - duration: 240
      arrivalRate: 50
      name: Sustained load

scenarios:
  - name: "Get hello."
    flow:
      - get:
          url: "/demo/hello"
