
config:
  target: "https://nginx.gmdemo.sandbox.ampc.axwaytest.net"
  tls:
    rejectUnauthorized: false
  #http:
  #  pool: 10
  http:
    pool: 10
    timeout: 30
  phases:
    - duration: 2400
      arrivalRate: 200
      name: Sustained load

scenarios:
  - name: "Get hello."
    flow:
      - get:
          url: "/demo/hello"
