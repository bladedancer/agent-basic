apiVersion: v1
kind: ConfigMap
metadata:
  name: ampgw-provisioning
  labels:
{{ include "ampgw.labels" . | indent 4 }}
data:
{{- range $key, $value := .Values.files }}
  {{ $key }}: |-
{{ $value | default "" | indent 4 }}
{{- end -}}
{{- range $key, $value := .Values.templates }}
  {{ $key }}: |-
{{ $valueWithDefault := default "" $value -}}
{{ tpl $valueWithDefault $ | indent 4 }}
{{- end -}}
